import {
  useSubmission,
  type RouteSectionProps
} from "@solidjs/router";

export default function Login(props: RouteSectionProps) {

  return (
    <main>
      <div>Sup bro</div>
    </main>
  );
}
